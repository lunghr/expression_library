{
  "name": "expression-editor",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev:demo": "npm run dev --prefix apps/demo-playground -- --host 127.0.0.1 --port 4173",
    "lint": "eslint .",
    "test:e2e": "playwright test",
    "test:unit": "npm run test --workspace @expression-editor/core && npm run test --workspace @expression-editor/adapters"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@playwright/test": "^1.53.1",
    "@types/node": "^25.6.0",
    "eslint": "^9.25.1",
    "globals": "^16.0.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.30.1"
  }
}
